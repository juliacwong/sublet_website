<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All housing photos
    </h1>

    <a href="/housing_photos/new" class="btn btn-block btn-outline-secondary">
      Add a new housing photo
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Photo caption
        </th>

        <th>
          Photo owner
        </th>

        <th>
          Housing
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @housing_photos.each do |housing_photo| %>
      <tr>
        <!-- Display housing_photo.id start -->
        <td>
          <%= housing_photo.id %>
        </td>
        <!-- Display housing_photo.id end -->

        <!-- Display housing_photo.photo_caption start -->
        <td>
          <%= housing_photo.photo_caption %>
        </td>
        <!-- Display housing_photo.photo_caption end -->

        <!-- Display housing_photo.photo_owner_id start -->
        <td>
          <% if housing_photo.photo_owner.present? %>
            <a href="/users/<%= housing_photo.photo_owner.id %>">
              <%= housing_photo.photo_owner.email %>
            </a>
          <% end %>
        </td>
        <!-- Display housing_photo.photo_owner_id end -->

        <!-- Display housing_photo.housing_id start -->
        <td>
          <% if housing_photo.housing.present? %>
            <a href="/housing_listings/<%= housing_photo.housing.id %>">
              <%= housing_photo.housing.housing_type %>
            </a>
          <% end %>
        </td>
        <!-- Display housing_photo.housing_id end -->

        <!-- Display housing_photo.created_at start -->
        <td>
          <%= time_ago_in_words(housing_photo.created_at) %> ago
        </td>
        <!-- Display housing_photo.created_at end -->

        <!-- Display housing_photo.updated_at start -->
        <td>
          <%= time_ago_in_words(housing_photo.updated_at) %> ago
        </td>
        <!-- Display housing_photo.updated_at end -->

        <td>
          <a href="/housing_photos/<%= housing_photo.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @housing_photos, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
